# Nvm commands

Check version:
```bash
node -v
```

List installed versions:
```bash
nvm ls
nvm ls-remote
```

Install specified version:
```bash
nvm install <version>
```

Switch version:
```bash
nvm use <version>
```

Set default version:
```bash
nvm alias default <version>
```
